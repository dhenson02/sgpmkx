/*
addEvent("hashchange", window, function() {
  var hash = window.location.hash.replace(/^#/, '');
  if (hash) {
    init(hash);
  }
  else {
    loadingSomething(false, content);
    newModal({
      title: "## Bummer",
      text: "Page doesn\'t exist :(",
      type: "error",
      showCancelButton: false
    });
    return false;
  }
});

*/
